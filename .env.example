# Noesis deployment config (single vertical per deployment)
NOESIS_ACTIVE_VERTICAL=regulatory
NOESIS_PROVIDER_MODE=replay          # replay (free) | record | live
NOESIS_LLM_MODEL=claude-sonnet-5
NOESIS_CASSETTE_ROOT=evals/cassettes
NOESIS_CORPUS_DSN=postgresql://user:pass@localhost:5432/noesis
# Secrets (record/live only — never commit real values):
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
TAVILY_API_KEY=
