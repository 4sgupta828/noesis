# Noesis deployment config (single vertical per deployment — O3)
NOESIS_ACTIVE_VERTICAL=regulatory
NOESIS_DATABASE_URL=postgresql+asyncpg://noesis:noesis@localhost:5432/noesis
# Secrets (never commit real values):
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
