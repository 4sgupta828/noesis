"""noesis_vertical_medical — vertical #2 (medical / biomedical evidence).

Reference of a genuinely different domain on the same kernel. ALL medical
vocabulary (trial/condition/intervention/NCT/phase), the ClinicalTrials.gov
connector, evidence-hierarchy authority policy, persona, and UI live HERE —
never in noesis_kernel.
"""
from .manifest import build_manifest

manifest = build_manifest()
